## Available Scripts

In the project directory, you can run:

### `npm run server` with  `cd .\frontend\` then `npm run dev`

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in your browser.

The page will reload when you make changes.\
You may also see any lint errors in the console.

### `npm run build`

Builds the app for production.\
It correctly bundles React in production mode and optimizes the build for the best performance.

### `npm run start`

Runs the app in production mode.\
Open [http://localhost:5000](http://localhost:5000) to view it in your browser.

## Preview

<img src="https://github.com/user-attachments/assets/8bc031f4-fda5-4b2d-b0de-1f06f8a112a4" alt="loginPage" width="1085">
<img src="https://github.com/user-attachments/assets/e4fe6484-f026-412f-9126-d323417db7e3" alt="coreApp" width="1085">
